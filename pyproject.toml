[project]
name = "insperity-mcp"
version = "0.1.0"
description = "MCP server for the Insperity REST API"
requires-python = ">=3.13"
dependencies = [
    "python-dotenv==1.2.1",
    "requests==2.32.5",
]
